defmodule Cardboard.Games.ActSequence do
  @moduledoc false
  use Ecto.Schema
  alias Ecto.Changeset
  alias Ecto.UUID

  # @primary_key {:id, :binary_id, autogenerate: true}
  schema "act_sequences" do
  end
end
