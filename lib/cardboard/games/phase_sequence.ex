defmodule Cardboard.Games.PhaseSequence do
  @moduledoc false
  use Ecto.Schema
  alias Ecto.Changeset
  alias Ecto.UUID

  # @primary_key {:id, :binary_id, autogenerate: true}
  schema "phase_sequences" do
  end
end
