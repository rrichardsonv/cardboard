defmodule Cardboard.Games.ActPhase do
  @moduledoc false
  use Ecto.Schema
  alias Ecto.Changeset
  alias Ecto.UUID

  # @primary_key {:id, :binary_id, autogenerate: true}
  schema "act_phases" do
  end
end
