defmodule Cardboard.Playing.RoundMove do
  @moduledoc false
  use Ecto.Schema
  alias Ecto.Changeset
  alias Ecto.UUID

  # @primary_key {:id, :binary_id, autogenerate: true}
  schema "round_moves" do
  end
end
